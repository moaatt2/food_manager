services:
  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_DB: test_catalog
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata:
